info:
  name: healthz
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/healthz
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
